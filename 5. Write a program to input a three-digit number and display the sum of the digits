#include<stdio.h>
int main()
{
    int Num,temp,sum=0;
    printf("Enter a number: ");
    scanf("%d",&Num);
   while(Num>0)
   {
       temp=Num%10;
       sum=sum+temp;
       Num=Num/10;
   }
   printf("Sum of Number is %d",sum);
}